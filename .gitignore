# Python bytecode
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
env/

# Distribution / packaging
*.egg-info/
dist/
build/

# Environment files
.env

# Server runtime
.server.pid
reports/
evals/results/
var/

# IDE
.idea/
.vscode/
*.swp

# Package manager lock
uv.lock

# 评测脚本的本地向量缓存 (每个 evals 子目录一份, 换 embedding 模型即失效)。
# 匹配任意前缀和 json/jsonl 两种格式 —— 逐个文件名列过两次都漏: 先漏了
# .lme_emb_cache.json (66MB), 改成 *emb_cache.json 后又漏了 .jsonl。
evals/**/*emb_cache.json*
