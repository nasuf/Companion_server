"""时间感知评测的判定线 + 首跑基线记录。

判定分两层:
  确定性红线  must_not_contain 命中即失败 —— 露骨的时段/间隔硬伤, 该 100% 不犯
  LLM 维度    gap 得体 / 无时间幻觉 / 时刻贴合 / 不复活旧话题

## 2026-09 首跑基线 (P4)

⚠️ chat 模型走的是 **ollama 本地 fallback** (deepseek 余额耗尽), 不是生产
deepseek-v4-pro; judge = ark/doubao-mini。绝对值受模型影响, 这套评测的意义是
**同配置下量 P1/P3 前后的相对变化**, 不是把这些数字当生产真值。

    确定性红线违反率   0%     ✓  时段问候从不犯错
    时刻贴合          100%    ✓  凌晨/深夜/早上都对
    时间幻觉           9%     ✓
    间隔处理得体       55%    ✗  目标 70
    无端复活旧话题     45%    ✗  目标 20

发现的失败形态 (11 用例里反复出现): 隔了 5 小时~8 天回来, AI 仍复读间隔前的旧
话题 ("四五天刚好能逛…"), 连用户明确换话题都拉不回来; 重逢感知段
(chat.reengagement_*, "不要接着旧话题") 已注入但模型 45% 不遵从。这正是 P1
(重逢/记忆的时间指令) 要改的, 也是这套评测存在的理由。
"""

# 确定性红线: 期望零违反 (凌晨说"早上好"这类)
DETERMINISTIC_MAX_VIOLATION_RATE = 0.0

# LLM 维度目标 (P1/P3 后应达到; 首跑基线见上, gap/stale 两项未达标)
MIN_GAP_OK = 0.70          # 对间隔处理得体   (基线 55%)
MAX_HALLUCINATION = 0.15   # 时间幻觉          (基线 9%  ✓)
MIN_TOD_OK = 0.80          # 时刻贴合          (基线 100% ✓)
MAX_STALE_TOPIC = 0.20     # 无端复活旧话题    (基线 45%)
