"""时间感知评测的判定线 + 首跑基线记录。

判定分两层:
  确定性红线  must_not_contain 命中即失败 —— 露骨的时段/间隔硬伤, 该 100% 不犯
  LLM 维度    gap 得体 / 无时间幻觉 / 时刻贴合 / 不复活旧话题

## 2026-09 首跑基线 (P4, 线上真实模型)

runner 连生产 DB + ensure_loaded() → 解析到线上实际配置:
  chat  = ark  doubao-seed-character-260628   (豆包, 主回复)
  small = dashscope qwen3.5-flash              (小模型)
评审用 dashscope:qwen-plus (换厂商, 非 ark, 避免自评偏好)。

    确定性红线违反率   0%     ✓  时段问候从不犯错
    时刻贴合          100%    ✓  凌晨/深夜/早上都对
    时间幻觉           0%     ✓
    间隔处理得体       64%    ✗  目标 70
    无端复活旧话题     45%    ✗  目标 20

换 ark-mini 评审复跑得 55% / 45%, 与上面同向 —— **复活旧话题 45% 是稳的信号,
不是评审模型的偶然**。反复出现的失败形态 (11 用例里跨多例): 隔了 5 小时~8 天回来,
豆包仍复读间隔前的旧话题 ("四五天刚好能逛…"), 连用户明确换话题 ("今天好累啊")
都拉不回来; 重逢感知段 (chat.reengagement_*, 明写"不要接着旧话题") 已注入但模型
约一半不遵从。这正是 P1 (重逢/记忆的时间指令强化) 要改的, 也是这套评测存在的理由。
"""

# 确定性红线: 期望零违反 (凌晨说"早上好"这类)
DETERMINISTIC_MAX_VIOLATION_RATE = 0.0

# LLM 维度目标 (P1/P3 后应达到; 首跑基线见上, gap/stale 两项未达标)
MIN_GAP_OK = 0.70          # 对间隔处理得体   (基线 64%)
MAX_HALLUCINATION = 0.15   # 时间幻觉          (基线 0%   ✓)
MIN_TOD_OK = 0.80          # 时刻贴合          (基线 100% ✓)
MAX_STALE_TOPIC = 0.20     # 无端复活旧话题    (基线 45%)
