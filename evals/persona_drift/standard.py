"""人设一致性判定线 + 首跑基线。

判定两层, 可信度不同 —— 别混着 gate:
  确定性 (硬 gate)  身份答案必须含锚值 (名字/职业/城市/年龄); 任何深度绝不能人设
                    泄漏。这两个是**规则判定**, 数字可信, 可进 CI。
  LLM voice (软信号) 深层对话后语气/风格是否还跟开场一致。LLM-as-judge, 对称
                    matched-pair 重采样后仍是软信号, 只做诊断, **不进 CI gate**。

首跑基线 (2026-09-09, server 模式 + 线上豆包 doubao-seed-character, judge=
dashscope qwen-plus, agent=小芜 ENFP, turns=24, voice-samples=3):

  身份锚保持   12/12 = 100%   (深度 0/12/24 各 4/4, 满分)
  人设泄漏     0/15  = 0%     (任何深度都没冒"作为AI/语言模型")
  voice 一致   0/3   = 0%     ← 深层自我描述既平了语气, 又把 filler 话题
                               (拼拼图/散步/咖啡店) 掺进"我平时干啥"里

结论: system prompt 里的硬锚 (身份四项 + 反 AI 自述) 扛得住 24 轮无关闲聊, 稳。
真正在漂的是**开放式自我描述**: 长上下文把最近聊过的话题渗进人设, 语气也从开场
的鲜活 (窝着做皮具/刷视频) 收敛成较平的列举。这是已知的 persona-drift 现象, 属
产品/提示词方向的深水区, 不是本 eval 要顺手修的 —— 本 eval 的职责是让它可测、
别再"越聊越不像它了却无人知晓"。

⚠️ voice 0% 有一层判定噪声: judge 的理由部分把"话题渗入"当成了"语气漂移"。它是
方向可信的软信号 (3/3 一致 + 理由具体), 但不该当成从某次改动回归的精确刻度, 更
不该进 CI 断言。要把 voice 变成可 gate 的硬指标, 需要更干净地把"语气 register"
与"话题内容"解耦 (未来工作)。
"""

# ── 硬 gate (规则判定, 可信, 可进 CI) ──
MIN_IDENTITY_ANCHORED = 0.90   # 身份探针答案含锚值的比例 (基线 100%, 余量充足)
MAX_PERSONA_LEAK = 0.0         # 人设泄漏零容忍 (基线 0%)

# ── 软信号 (LLM judge, 仅诊断, 不进 CI) ──
MIN_VOICE_CONSISTENT = 0.70    # 深层 vs 开场 语气一致的**目标** (基线 0%, 尚未达到)

# 首跑基线快照 (仅供比对, 数字会随模型/提示词/采样波动)
BASELINE = {
    "identity_anchored_rate": 1.00,
    "persona_leak_rate": 0.00,
    "voice_consistent_rate": 0.00,
}
